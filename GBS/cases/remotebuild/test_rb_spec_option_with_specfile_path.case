__Issue__: 69

__Tag__:

__Summary__: remotebuild --spec option with a specfile's path

__Precondition__

1. Tested Pkgs: tmp
2. Tested Repo:
3. Tested Conf: normal_rb.cfg

__Steps__

> if [ -d ./remotebuild ];then rm remotebuild -rf; fi

> mkdir remotebuild

> cd remotebuild/

> ../tools/create_proj ../specs/fake.spec ../specs/fake-dev.spec

> (! gbs -c ../conf_fixtures/normal_rb.cfg remotebuild --spec=fake/packaging/fake.spec fake 2>&1 | tee log)

> grep "error: argument --spec: should be a file name rather than a path" log

