__Issue__:

__Tag__:

__Version__: 0.8

__Summary__: test -T option

__Precondition__

1. Tested Pkgs: fake
2. Tested Repo:
3. Tested Conf: noraml_rb.cfg

__Steps__

> source tools/gbs_vars

> if [ -d ./remotebuild ];then rm remotebuild -rf; fi

> mkdir remotebuild

> cd remotebuild

> ../tools/create_proj ../specs/fake.spec

> gbs -c ../conf_fixtures/normal_rb.cfg remotebuild -T $GBS_RB_TARGET fake

> osc -c ../conf_fixtures/oscrc rdelete --recursive $GBS_RB_TARGET -m "delete target prj"
