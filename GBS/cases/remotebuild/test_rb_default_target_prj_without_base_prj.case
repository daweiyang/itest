__Issue__: 36

__Tag__: 0.12

__Summary__: use default target prj name if base prj is not specified

__Precondition__

1. Tested Pkgs: fake
2. Tested Repo:
3. Tested Conf: tizendev.cfg

__Steps__

> if [ -d ./remotebuild ];then rm remotebuild -rf; fi

> mkdir remotebuild

> cd remotebuild

> ../tools/create_proj ../specs/fake.spec

> gbs -c ../conf_fixtures/tizendev.cfg remotebuild fake 2>&1 | tee log

> grep -E "checking status of obs project: home:.*:gbs[^:]" log
