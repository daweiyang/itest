__Issue__:

__Tag__:

__Summary__: specify a commit to remotebuild(tag)

__Precondition__

1. Tested Pkgs: mlite
2. Tested Repo:
3. Tested Conf: normal_rb.cfg

__Steps__

> source tools/gbs_vars

> if [ -d ./remotebuild ];then rm remotebuild -rf; fi

> mkdir remotebuild

> cd remotebuild

> ../tools/create_proj ../specs/fake.spec

> cd fake

> git tag tizen

> gbs -c ../../conf_fixtures/normal_rb.cfg remotebuild -T $GBS_RB_TARGET -c tizen 2>&1 | tee log

> grep -E -i "Creating.* source archive.*from 'tizen'" log

> osc -c ../../conf_fixtures/oscrc rdelete --recursive $GBS_RB_TARGET -m "delete target prj"
