__Issue__: 59

__Tag__:

__Summary__: remotebuild --status with -B with non_exist prj name

__Precondition__

1. Tested Pkgs: fake
2. Tested Repo:
3. Tested Conf: normal_rb.cfg

__Steps__

> source tools/gbs_vars

> if [ -d ./remotebuild ];then rm remotebuild -rf; fi

> mkdir remotebuild

> cd remotebuild

> ../tools/create_proj ../specs/fake.spec

> (! gbs -c ../conf_fixtures/normal_rb.cfg remotebuild -B NonExistsPrj -T $GBS_RB_TARGET --status fake 2>&1 | tee log)

> grep -E -i "error.*can't get.*build results" log
