__Issue__:

__Tag__:

__Version__:

__Summary__: spec file has no source tag

__Precondition__

1. Tested Pkgs: xorg-x11-filesystem
2. Tested Repo:
3. Tested Conf: normal_rb.cfg

__Steps__

> source tools/gbs_vars

> if [ -d ./remotebuild ];then rm remotebuild -rf; fi

> mkdir remotebuild

> cd remotebuild

> ../tools/create_proj ../specs/xorg-x11-filesystem.spec

> gbs -c ../conf_fixtures/normal_rb.cfg remotebuild -T $GBS_RB_TARGET xorg-x11-filesystem

> osc -c ../conf_fixtures/oscrc rdelete --recursive $GBS_RB_TARGET -m "delete target prj"
