__Issue__: 35

__Tag__:

__Summary__: invalid build server 2

__Precondition__

1. Tested Pkgs: fake
2. Tested Repo:
3. Tested Conf: invalid_build_server_2.cfg

__Steps__

> if [ -d ./remotebuild ];then rm remotebuild -rf; fi

> mkdir remotebuild

> cd remotebuild

> ../tools/create_proj ../specs/fake.spec

> (! gbs -c ../conf_fixtures/invalid_build_server_2.cfg remotebuild fake 2>&1 | tee log)

> grep -E -i "error.*invalid service apiurl" log

