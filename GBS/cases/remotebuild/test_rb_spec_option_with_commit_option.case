__Issue__: 69, 83

__Tag__:

__Summary__: remotebuild --spec option with commit option

__Precondition__

1. Tested Pkgs: fake
2. Tested Repo:
3. Tested Conf: normal_rb.cfg

__Steps__

> source tools/gbs_vars

> if [ -d ./remotebuild ];then rm remotebuild -rf; fi

> mkdir remotebuild

> cd remotebuild/

> ../tools/create_proj ../specs/fake.spec

> cd fake

> sed -i -r 's/(%\{name\}-%\{version\})\.tbz2/\1\.lzma/' packaging/fake.spec

> git commit -am "change source to *.lzma"

> gbs -c ../../conf_fixtures/normal_rb.cfg remotebuild -T $GBS_RB_TARGET --spec=fake.spec -c HEAD^ 2>&1 | tee log

> grep -E "Creating \(native\) source archive fake.*\.tbz2 from 'HEAD\^'" log

> grep "follow the link to monitor the build progress:" log

> osc -c ../../conf_fixtures/oscrc rdelete --recursive $GBS_RB_TARGET -m "delete target prj"

