__Issue__:

__Tag__:

__Summary__: invalid spec file

__Precondition__

1. Tested Pkgs: tizen-accelerator
2. Tested Repo:
3. Tested Conf: normal_rb.cfg

__Steps__

> if [ -d ./remotebuild ];then rm remotebuild -rf; fi

> mkdir remotebuild

> cd remotebuild

> ../tools/create_proj ../specs/tizen-accelerator.spec

> (! gbs -c ../conf_fixtures/normal_rb.cfg remotebuild tizen-accelerator 2>&1 | tee log)

> grep -E -i "error.* can't parse specfile" log

