__Issue__: 59

__Tag__:

__Summary__: --status failed test

__Precondition__

1. Tested Pkgs: pkgmgr
2. Tested Repo:
3. Tested Conf: normal_rb.cfg

__Steps__

> if [ -d ./remotebuild ];then rm remotebuild -rf; fi

> mkdir remotebuild

> cd remotebuild

> ../tools/create_proj ../specs/pkgmgr.spec

> (! gbs -c ../conf_fixtures/normal_rb.cfg remotebuild --status pkgmgr/ 2>&1 | tee log)

> grep -E -i "error.*can't get" log

