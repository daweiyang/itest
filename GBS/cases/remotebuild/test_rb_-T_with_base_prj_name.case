__Issue__:

__Tag__:

__Summary__: specify -T with a base prj jname

__Precondition__

1. Tested Pkgs: fake
2. Tested Repo:
3. Tested Conf: normal_rb.cfg

__Steps__

> if [ -d ./remotebuild ];then rm remotebuild -rf; fi

> mkdir remotebuild

> cd remotebuild

> ../tools/create_proj ../specs/fake.spec

> (! gbs -c ../conf_fixtures/normal_rb.cfg remotebuild -T Tizen:Main fake 2>&1 | tee log)

> grep -E "error.*can't create.*" log
