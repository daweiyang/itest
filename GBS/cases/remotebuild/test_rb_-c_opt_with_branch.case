__Issue__:

__Tag__:

__Summary__: specify a commit to remotebuild(branch)

__Precondition__

1. Tested Pkgs: fake
2. Tested Repo:
3. Tested Conf: normal_rb.cfg

__Steps__

> source tools/gbs_vars

> if [ -d ./remotebuild ];then rm remotebuild -rf; fi

> mkdir remotebuild

> cd remotebuild

> ../tools/create_proj ../specs/fake.spec

> gbs -c ../conf_fixtures/normal_rb.cfg remotebuild -c master -T $GBS_RB_TARGET fake 2>&1|tee log

> grep -i -E "Creating.*source archive.* from 'master'" log

> grep -i -E "follow the link to monitor the build progress" log

> osc -c ../conf_fixtures/oscrc rdelete --recursive $GBS_RB_TARGET -m "del target prj"
