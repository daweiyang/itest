__Issue__:

__Tag__:

__Summary__: specfile dosen't exist

__Precondition__

1. Tested Pkgs: build-compare
2. Tested Repo:
3. Tested Conf: normal_rb.cfg

__Steps__

> if [ -d ./remotebuild ];then rm remotebuild -rf; fi

> mkdir remotebuild

> cd remotebuild

> ../tools/create_proj ../specs/fake.spec

> cd fake

> rm -f packaging/fake.spec

> git rm packaging/fake.spec

> git commit -am "delte specfile"

> (! gbs -c ../../conf_fixtures/normal_rb.cfg remotebuild 2>&1 | tee log)

> grep -E -i "error.*can't find any spec file" log
