__Issue__:

__Tag__:

__Summary__: --include-all test

__Precondition__

1. Tested Pkgs: fake
2. Tested Repo:
3. Tested Conf: normal_rb.cfg

__Steps__

> source tools/gbs_vars

> if [ -d ./remotebuild ];then rm remotebuild -rf; fi

> mkdir remotebuild

> cd remotebuild

> ../tools/create_proj ../specs/fake.spec

> cd fake

> touch 1

> gbs -c ../../conf_fixtures/normal_rb.cfg remotebuild --include-all -T $GBS_RB_TARGET 2>&1 | tee log

> grep -E "the following untracked files would be included" log

> osc -c ../../conf_fixtures/oscrc rdelete --recursive $GBS_RB_TARGET -m "delete target prj"
