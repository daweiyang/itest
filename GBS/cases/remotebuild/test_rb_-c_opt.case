__Issue__:

__Tag__:

__Summary__: specify a commit to remotebuild

__Precondition__

1. Tested Pkgs: mlite
2. Tested Repo:
3. Tested Conf: normal_rb.cfg

__Steps__

> source tools/gbs_vars

> ./tools/create_proj ./specs/fake.spec

> cd fake

> git rm -r packaging

> git commit -m 'remove package dir, then build HEAD will fail, but HEAD^ is fine'

> gbs -c ../conf_fixtures/normal_rb.cfg remotebuild -c HEAD^ -T $GBS_RB_TARGET

> osc -c ../conf_fixtures/oscrc rdelete --recursive $GBS_RB_TARGET -m "delete target prj"
