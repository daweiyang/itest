__Issue__:

__Tag__: 0.12

__Summary__: set repos manually if no base prj

__Precondition__

1. Tested Pkgs: fake
2. Tested Repo:
3. Tested Conf: tizendev.cfg

__Steps__

> source tools/gbs_vars

> if [ -d ./remotebuild ];then rm remotebuild -rf; fi

> mkdir remotebuild

> cd remotebuild

> ../tools/create_proj ../specs/fake.spec

> gbs -c ../conf_fixtures/normal_rb.cfg remotebuild fake -T $GBS_RB_TARGET 2>&1 | tee log

> grep "please add repos from OBS webUI manually" log

> osc -c ../conf_fixtures/oscrc rdelete --recursive $GBS_RB_TARGET -m "delete target prj"
