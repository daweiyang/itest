__Issue__: 69

__Tag__: 0.12

__Summary__: remotebuild with --spec option

__Precondition__

1. Tested Pkgs: fake
2. Tested Repo:
3. Tested Conf: normal_rb.cfg

__Steps__

> source tools/gbs_vars

> if [ -d ./remotebuild ];then rm remotebuild -rf; fi

> mkdir remotebuild

> cd remotebuild

> ../tools/create_proj ../specs/fake.spec ../specs/fake-dev.spec

> cd fake

> gbs -c ../../conf_fixtures/normal_rb.cfg remotebuild -T $GBS_RB_TARGET --spec=fake.spec 2>&1 | tee log

> grep -E "Creating .* fake.*\.tbz2" log

> grep "follow the link to monitor the build progress:" log

> osc -c ../../conf_fixtures/oscrc rdelete --recursive $GBS_RB_TARGET -m "delete target prj"
