__Issue__: 82

__Tag__:

__Summary__: submit not on a branch

__Precondition__

1. Tested Pkgs: test-submit
2. Tested Repo:
3. Tested Conf:

__Steps__

> if [ -d 'fake-submit' ]; then rm -rf fake-submit; fi

> if [ -d 'test-submit' ]; then rm -rf test-submit; fi

> tools/create_proj specs/fake.spec -C fake-submit

> git clone fake-submit test-submit

> pushd test-submit

> git checkout --detach

> git branch

> (! gbs submit -m 'test not on a branch' 2>&1 | tee log)

> test -n "`grep "error:.*Currently not on a branch" log`"

