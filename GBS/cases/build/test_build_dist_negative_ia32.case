__Issue__:

__Tag__:

__Summary__:test dist feature with wrong conf

__Precondition__

1. Tested Pkgs:
2. Tested Repo: https://download.tz.otcshare.org/snapshots/trunk/common/latest
3. Tested Conf: conf_fixtures/tizendev.cfg

__Steps__

> sudo rm ~/GBS-ROOT/local/repos -rf

> rm build -rf

> mkdir build

> cd build/

> ../tools/create_proj ../specs/xorg-x11-filesystem.spec

> touch tizen.conf

# using am empty build conf, it will fail
> (! gbs -c ../conf_fixtures/tizendev.cfg build --dist ./tizen.conf -A ia32 2>&1 | tee log)

#buildroot is destroyed, for other cases, delete buildroot
> sudo rm ~/GBS-ROOT/local/*scratch*0* -rf

> test -z "`grep "mount:.*scratch" log`"

> cd xorg-x11-filesystem && test -n "`git status | grep "working directory clean"`" && cd ..
