__Issue__:527

__Tag__:

__Summary__: support building x86_64 in x86_64 machine

__Precondition__

1. Tested Pkgs:
2. Tested Repo:
3. Tested Conf:

__Steps__

> tools/create_proj -f specs/fake.spec

> rm -rf ~/GBS-ROOT/local/repos/

> hostarch=$(uname -p)

> if [ $hostarch != x86_64 ]; then

> (! gbs -c conf_fixtures/tz_pc.cfg build -A x86_64 fake 2>&1 | tee log)

> grep "can't build target arch x86_64" log

> else

> gbs -c conf_fixtures/tz_pc.cfg build -A x86_64 fake 2>&1 | tee log

> test -f ~/GBS-ROOT/local/repos/tizenpc/x86_64/RPMS/fake-1.0-1.x86_64.rpm

> test -f ~/GBS-ROOT/local/repos/tizenpc/x86_64/SRPMS/fake-1.0-1.src.rpm

> fi
