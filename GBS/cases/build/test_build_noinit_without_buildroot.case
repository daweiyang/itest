__Issue__:

__Tag__:

__Summary__:test noinit feature with buildroot deleted

__Precondition__

1. Tested Pkgs:
2. Tested Repo: https://download.tz.otcshare.org/snapshots/trunk/common/latest
3. Tested Conf: conf_fixtures/tizendev.cfg

__Steps__

> sudo rm ~/GBS-ROOT/local/scratch.i* -rf

> rm build -rf

> mkdir build

> cd build/

> ../tools/create_proj ../specs/fake.spec

> (! gbs -c ../conf_fixtures/tizendev.cfg build --noinit -A ia32 fake 2>&1 | tee log)

> grep "build root:.* does not exist\. Please build without --noinit first" log

> cd fake && test -n "`git status | grep "working directory clean"`" && cd ..
