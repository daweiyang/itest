__Issue__: 673

__Tag__:

__Summary__:spec BuildArch and -A specified, generate BuildArch rpm

__Precondition__

1. Tested Pkgs:
2. Tested Repo: https://download.tz.otcshare.org/snapshots/trunk/common/latest
3. Tested Conf: conf_fixtures/tizendev.cfg

__Steps__

> . tools/assert

> sudo rm $GBS_LOCAL_REPOS -rf

> tools/create_proj specs/fake.spec

> gbs -c ./conf_fixtures/tizendev.cfg build -A i586 2>&1| tee log

#The target arch can be specified by depanneur --target i586.
> AssertRPMsIWith fake| grep i586

> sudo rm $GBS_LOCAL_REPOS -rf

> sed -i "/^Source/a BuildArch: i386" fake/packaging/fake.spec

> cd fake && git commit -am 'change buildarch' && cd ..

> gbs -c ./conf_fixtures/tizendev.cfg build -A i586 2>&1| tee log

> AssertRPMsIWith fake| grep i386

#The following is True. however, ia32 is deprecated.
#> sudo rm $GBS_LOCAL_REPOS -rf
#
#> gbs -c ./conf_fixtures/tizendev.cfg build -A ia32 2>&1| tee log
#
#> AssertRPMsIWith fake| grep i386
