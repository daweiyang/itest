__Issue__:119

__Tag__:

__Summary__:specify extra packages installed in buildroot with --extra-packs

__Precondition__

1. Tested Pkgs:
2. Tested Repo: https://download.tz.otcshare.org/snapshots/trunk/common/latest
3. Tested Conf: conf_fixtures/tizendev.cfg

__Steps__

> sudo rm ~/GBS-ROOT/local/repos -rf

#rm build root for checking install packages later
> sudo rm ~/GBS-ROOT/local/scratch.i586.0 -rf

> rm build -rf

> mkdir build

> cd build

> ../tools/create_proj ../specs/xorg-x11-filesystem.spec

> gbs -c ../conf_fixtures/tizendev.cfg build --extra-packs=giflib,smack xorg-x11-filesystem -A ia32 2>&1 | tee log

> test -n "`awk '/init_buildsystem/&&/giflib/&&/smack/' log`"

#check source package unchanged
> cd xorg-x11-filesystem && test -n "`git status | grep "working directory clean"`" && cd ..

> test -n "`find ~/GBS-ROOT/local/repos -name "*xorg-x11-filesystem*.rpm"`"

#check extra-packs packages installed in build root
> test -n "`sudo chroot ~/GBS-ROOT/local/scratch.i586.0 rpm -q giflib | grep "giflib-"`"

> test -n "`sudo chroot ~/GBS-ROOT/local/scratch.i586.0 rpm -q smack | grep "smack-"`"

