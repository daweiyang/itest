__Issue__: 

__Tag__:

__Summary__: gbs build incremental with --spec which specfile not exist

__Precondition__

1. Tested Pkgs:
2. Tested Repo: https://download.tz.otcshare.org/snapshots/trunk/common/latest
3. Tested Conf: conf_fixtures/tizendev.cfg

__Steps__

> . ./tools/assert

> sudo rm $GBS_LOCAL_REPOS -rf

> tools/create_proj specs/fake.spec

> (! gbs -c conf_fixtures/tizendev.cfg build --incremental --spec=no.spec -A ia32 fake 2>&1| tee log)

> (! grep "/usr/bin/depanneur" log)

> grep "error.*no.spec.*not exist" log
