__Issue__: 365

__Tag__:

__Summary__:change arch to build, not skip build anymore

__Precondition__

1. Tested Pkgs: sw-tools
2. Tested Repo: https://download.tz.otcshare.org/snapshots/trunk/common/latest
3. Tested Conf: conf_fixtures/tizendev.cfg

__Steps__

> . tools/assert

> sudo rm ${GBS_LOCAL_REPOS} -rf

> tools/create_proj specs/sw-tools.spec

> gbs -c conf_fixtures/tizendev.cfg build sw-tools -A ia32 2>&1 | tee log

> gbs -c conf_fixtures/tizendev.cfg build sw-tools -A ia32 2>&1 | tee log

#check arch not changed, skip still
> test -n "`grep "skip.*sw-tools" log`"

> gbs -c conf_fixtures/tizendev.cfg build sw-tools -A armv7l 2>&1 | tee log

#check arch changed, no skip any more
> test -z "`grep "skip.*sw-tools" log`"

#check rpm and src.rpm now under each arch
> AssertRPMsIWith sw-tools

> AssertRPMsAWith sw-tools
