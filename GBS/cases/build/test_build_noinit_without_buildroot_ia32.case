__Issue__:

__Tag__:

__Summary__:test noinit feature with buildroot deleted

__Precondition__

1. Tested Pkgs:
2. Tested Repo: https://download.tz.otcshare.org/snapshots/trunk/common/latest
3. Tested Conf: conf_fixtures/tizendev.cfg

__Steps__

> sudo rm ~/GBS-ROOT/local/repos -rf

> sudo rm ~/GBS-ROOT/local/scratch*i586* -rf

> rm build -rf

> mkdir build

> cd build/

> ../tools/create_proj ../specs/sw-tools.spec

> (! gbs -c ../conf_fixtures/tizendev.cfg build --noinit -A ia32 2>&1 | tee log)

> test -z "`grep "generate repositories" log`"

> cd sw-tools && test -n "`git status | grep "working directory clean"`" && cd ..

> test -n "`grep "error.*scratch.*does not exist" log`"

