__Issue__: 685

__Tag__:

__Summary__: buildroot uid gid changed,gbs build still work

__Precondition__

1. Tested Pkgs:
2. Tested Repo: https://download.tz.otcshare.org/snapshots/trunk/common/latest
3. Tested Conf: conf_fixtures/tizendev.cfg

__Steps__

> ./tools/create_proj ./specs/fake.spec

> gbs -c ./conf_fixtures/tizendev.cfg build -A i586 --buildroot=buildroot fake 2>&1

> BUILD_ROOT=buildroot/local/scratch.i586.0

> ABUILD_UID=666666

> ABUILD_GID=888888

> sudo sed -i '/^abuild/d' $BUILD_ROOT/etc/passwd

> sudo sed -i '/^abuild/d' $BUILD_ROOT/etc/group

> echo "abuild:x:${ABUILD_UID}:${ABUILD_GID}:Autobuild:/home/abuild:/bin/bash" |sudo tee -a $BUILD_ROOT/etc/passwd

> echo "abuild:x:${ABUILD_GID}:" |sudo tee -a $BUILD_ROOT/etc/group

> gbs -c ./conf_fixtures/tizendev.cfg build -A i586 --buildroot=buildroot --overwrite fake 2>&1
