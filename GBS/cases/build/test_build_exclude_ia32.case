__Issue__:

__Tag__:

__Summary__: test exclude feature

__Precondition__

1. Tested Pkgs:
2. Tested Repo: https://download.tz.otcshare.org/snapshots/trunk/common/latest
3. Tested Conf: conf_fixtures/tizendev.cfg

__Steps__


> if [ -d ~/GBS-ROOT ]; then sudo rm ~/GBS-ROOT/local/repos -rf; fi

> if [ -d ./build ]; then rm build -rf; fi

> mkdir build

> cd build/

> ../tools/create_proj ../specs/giflib.spec

> ../tools/create_proj -C power ../specs/capi-system-power.spec

> ../tools/create_proj ../specs/xorg-x11-filesystem.spec

> gbs -c ../conf_fixtures/tizendev.cfg build --exclude=power,giflib -A ia32 2>&1 | tee log

> test -z "`grep "mount:.*scratch" log`"

> cd giflib && test -n "`git status | grep "working directory clean"`" && cd ..

> cd power && test -n "`git status | grep "working directory clean"`" && cd ..

> cd xorg-x11-filesystem && test -n "`git status | grep "working directory clean"`" && cd ..

> test -z "`find ~/GBS-ROOT/local/repos -name "*giflib*.rpm"`"

> test -z "`find ~/GBS-ROOT/local/repos -name "*power*.rpm"`"

> test -n "`find ~/GBS-ROOT/local/repos -name "*xorg-x11-filesystem*.rpm"`"

