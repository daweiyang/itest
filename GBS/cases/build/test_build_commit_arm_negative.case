__Issue__:

__Tag__:

__Summary__:test commit feature with multi-specs

__Precondition__

1. Tested Pkgs:
2. Tested Repo: https://download.tz.otcshare.org/snapshots/trunk/common/latest
3. Tested Conf: conf_fixtures/tizendev.cfg

__Steps__

> if [ -d ~/GBS-ROOT/local/repos ];then sudo rm ~/GBS-ROOT/local/repos -rf; fi

> if [ -d ./build ];then rm build -rf; fi

> mkdir build

> cd build/

> ../tools/create_proj ../specs/fake.spec ../specs/fake-dev.spec

> touch fake/xyz && cd fake && git add xyz && git commit -m "add xyz" && cd ..

> (! gbs -c ../conf_fixtures/tizendev.cfg build -c HEAD~1 fake -A armv7l 2>&1 | tee log)

> test -z "`grep "mount:.*scratch" log`"

> cd fake && test -n "`git status | grep "working directory clean"`" && cd ..

> test -n "`grep "error:.*multiple packages" log`"

